# Auto-generated by jaaicode. Per-project prompt history and other
# local state. Do NOT commit prompts — they often include unreleased
# plans, debug snippets, or transient API keys typed by accident.
#
# To check this folder into git anyway (e.g. to share a project-wide
# config), delete this file or whitelist specific files with `!name`.

*
!.gitignore
