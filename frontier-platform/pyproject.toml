[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "frontier-platform"
version = "0.0.1"
description = "Architecture-only blueprint for a frontier LLM training platform"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
