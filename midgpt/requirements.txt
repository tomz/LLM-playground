torch>=2.1
numpy
tiktoken>=0.6
datasets>=2.18
pyyaml
tqdm
requests
# optional
# wandb
# Optional throughput add-on (config-gated, off by default):
#   liger-kernel — fused linear-cross-entropy; set fused_ce: true (GPU+Triton).
#   Avoids materializing the [B*T, vocab] logits; exact, ~20% faster / ~60% less
#   memory. Install only if you enable it (pulls heavier Triton/CUDA deps).
# liger-kernel>=0.4
